services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped

  # Phase 2 — uncomment to add backend
  # backend:
  #   build:
  #     context: ./server
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3001:3001"
  #   environment:
  #     - NODE_ENV=production
  #     - MONGO_URI=mongodb://mongo:27017/portfolio
  #   depends_on:
  #     - mongo
  #   restart: unless-stopped

  # Phase 2 — uncomment to add database
  # mongo:
  #   image: mongo:7
  #   volumes:
  #     - mongo_data:/data/db
  #   restart: unless-stopped

# volumes:
#   mongo_data:
